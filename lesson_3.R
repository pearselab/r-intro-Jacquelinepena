# Cat class 
pumpkin <- list(length=4,weight=20,race="bengal")
class(pumpkin) <- "cat"
new.dog <- function(weight, breed){
output <- list(weight=weight, breed=breed)
class(output) <- "cat"
  
sequence <- seq(1:20)
class(point) <- (x=4, y=9)

